<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mybatis.mapper.EmployeeMapper">
	<resultMap id="EmployeeResult" type="Employee">
		<id property="id" column="employee_id" />
		<result property="firstName" column="first_name" />
		<result property="lastName" column="last_name" />
		<result property="email" column="email" />
		<result property="phoneNumber" column="phone_number" />
		<result property="hireDate" column="hire_date" />
		<result property="job" column="job_id" />
		<result property="salary" column="salary" />
		<result property="commissionPct" column="commission_pct" />
		<result property="manager" column="manager_id" />
		<result property="department" column="department_id" />
	</resultMap>
	<sql id="selectAll">
		SELECT
		*
		FROM employees
	</sql>
	<select id="findEmployeesByName" resultMap="EmployeeResult">
		<include refid="selectAll" /> WHERE first_name LIKE
		CONCAT('%',#{firstName},'%') AND last_name LIKE
		CONCAT('%',#{lastName},'%') </select>
	<insert id="addEmployee" parameterType="Employee" keyProperty="id"
		keyColumn="employee_id" useGeneratedKeys="true">
		INSERT INTO employees
		(first_name,last_name,email,phone_number,hire_date,job_id,salary,commission_pct,manager_id,department_id)
		VALUES
		(#{firstName},#{lastName},#{email},#{phoneNumber},#{hireDate},#{job},#{salary},#{commissionPct},#{manager},#{department})
	</insert>
	<select id="getEmployee" resultMap="EmployeeResult">
		SELECT
		*
		FROM employees
		WHERE employee_id =
		#{id}
	</select>
	<update id="chgEmployee" parameterType="Employee">
		UPDATE employees SET
		first_name = #{firstName},
		last_name = #{lastName},
		email = #{email},
		phone_number = #{phoneNumber},
		hire_date = #{hireDate},
		job_id = #{job},
		salary = #{salary},
		commission_pct = #{commissionPct},
		manager_id = #{manager},
		department_id = #{department}
		WHERE employee_id = #{id}
	</update>
	<delete id="delEmployee">
		delete from employees where employee_id=#{id}
	</delete>
</mapper>